{
  "name": "tinte-figma-plugin",
  "version": "0.1.0",
  "description": "Sync Tinte themes to Figma variables",
  "scripts": {
    "build": "tsc && cp src/ui.html dist/",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "typescript": "^5"
  }
}
